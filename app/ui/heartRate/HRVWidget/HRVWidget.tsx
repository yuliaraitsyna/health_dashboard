"use server"

import React from 'react'

export default async function HRVWidget() {
    // const data = await fetch('/api/heart_rate/variability').then(res => res.json());

    // console.log(data)
  return (
    <></>
  )
}
